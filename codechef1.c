#include <stdio.h>
#include <string.h>

typedef long long int ll;
char c[40000000];

int main(){
	ll d;
	scanf("%lld",&d);
    char ch;
    scanf("%c",&ch);
    char s[1000];
    while(d--){
         gets(s);
        //    puts(s);
    }
	gets(c);
    c[strlen(c)]='.';
	puts(c);
}
